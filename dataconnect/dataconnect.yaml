specVersion: "v1"
serviceId: "personal"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "personal-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
